# Prompt Contracts

Write stable policy and tool rules before task data. Delimit untrusted documents and tool output as data. Use a schema and deterministic validator for structured output; do not ask a model to judge whether JSON is valid. Add examples only when a regression case proves that clearer instructions did not resolve the ambiguity.
