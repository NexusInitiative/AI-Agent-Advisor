# Coordination Patterns

Use a manager-owned task record with goal, scope, evidence, budget, and terminal status. Make handoffs typed and idempotent. Run workers in parallel only when outputs are independent; otherwise keep the task in one context. Test routing, handoff completeness, tool permissions, and merge decisions separately from final-answer quality.
