# Embedding Evaluation

Evaluate retrieval with representative queries, relevance labels, language splits, and filters. Keep preprocessing, query/document formatting, index metric, and reranking fixed while comparing models. Re-embed and rebuild every index when the model, dimension, or normalization changes. Combine dense retrieval with lexical search for exact identifiers and codes.
