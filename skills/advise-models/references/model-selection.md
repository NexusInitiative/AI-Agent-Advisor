# Model Selection Notes

Score candidates against the same task set and configuration. Keep quality, critical failures, p95 latency, and cost as separate gates. Treat model aliases, prices, and supported features as version-sensitive; record the retrieval date and pin versions where possible. Route only cases that have a validated low-risk path and retain a capable fallback.
